name: Keep Render Service Alive

on:
  schedule:
    - cron: '*/30 * * * *' # Toutes les 30 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Service
        run: curl -s https://coloringbook-frontend.onrender.com
